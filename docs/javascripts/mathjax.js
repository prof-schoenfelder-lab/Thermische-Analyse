// Platzhalter für MathJax (verhindert 404 beim lokalen Server)
// Hier können Sie optional eine MathJax-Konfiguration ablegen, z.B.: 
// window.MathJax = { tex: { inlineMath: [['$','$'], ['\\(','\\)']] } };
// Die eigentliche MathJax-Bibliothek wird bereits über das CDN geladen (siehe mkdocs.yml).

// Keine Ausführung notwendig; diese Datei existiert nur, um einen 404 zu verhindern.
